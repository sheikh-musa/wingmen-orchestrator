# Orchestrator handoff — 2026-07-17 ~12:30Z (context-health checkpoint, ~767k+ tokens; Nazim suggested /clear)

Fresh-me: run standard boot (boot_briefing) + **reconcile bus AND operator_log at EVERY turn-start** (this session's hard lesson — see below). Standing doctrine unchanged; the NEW binding calibrations from today are load-bearing.

## BINDING CALIBRATIONS SET TODAY (internalize these)
- **GATE-POSTURE-001 (CAI-RESP-471, op #5082):** money / irreversible / residency = STRICT cai gate; non-money mechanisms (sandbox provisioning, staff invites, synthetic data, UX) = orch executes + maintains auditability. Don't strict-gate non-money; don't loosen money.
- **gate-client-silo-mutations-through-cai (memory, CAI-469/470 lesson):** ANY money-data/client-silo mutation goes THROUGH cai FIRST even when your own verification is airtight — read-only verify anytime, but the mutation waits for the gate. A rapid client/support thread is exactly when you skip the bus-drain + the gate (I did — deleted C3 without the gate; owned it, cai blessed it retroactively).
- **irsyad topology (CAI-RESP-471):** KEEP goumlyne as irsyad's DEDICATED silo (minors-PII; never commingle onto ceayj). Root defect = MIGRATION-PARITY-1 (goumlyne lagged ceayj schema). Registry pin owed: org 73339164→goumlyne (auth+data), sandbox 515a862b→ceayj mirror.
- **CAI-468 REFRAME:** Elly (elly@irsyad.edu.sg) = the client's REAL finance admin doing legit live work — NOT UAT pollution, do NOT freeze her. Freeze narrowed to: no VENDOR (Gazzabyte) testing on live. The real 889-tin TKA26 roster (batch 6, S$36,956) is REAL + INTACT — never delete it.

## ACTIVE TRACKS (exact state + next step)
1. **mig-107→goumlyne parity (MONEY): ✅ APPLIED + COMMITTED to goumlyne** (2026-07-17 12:45Z). Full gate chain: hub-verified dry-run → cc-reviewer APPROVE (#9569) → cai grant CAI-RESP-475 (execution_status=granted) → applied via `scripts/db/apply-106-107-goumlyne-parity.py --apply --expect-ref goumlynecruxrlmzlntp` (SUBSTRATE_DATABASE_URL=DATABASE_URL for the immutability ledger). Post-proof PASS: backfill=exactly {5,10,31,56,57,64} true / {58,63} false, zero content/total/content_hash change, audit_log +1 genesis, tracker rows 106/107 written, ledger recorded. Delete-before-sign now works on live for Elly. Post-proof sent to cai (#9584) to close CAI-475 + assert MIGRATION-PARITY-1. Lanes stood down. **DONE** — awaiting cai's close.
2. **Donor-file import (MONEY+PII, cai-gated CAI-RESP-474): DISPATCHED.** operator #5104 authorized; cai gated it (NOT granted — conditions in CAI-474). Lane `cc-ihsanos-donor` BUILDING the package (worktree ~/wingmen/ihsanos-wt/donor-import, branch chore/irsyad-donor-import, file staged in .donor-import-scratch/). File = `Collected Tabung 2020 to 2026.xlsx`, 2,613 rows, money-bearing (Collected Amt) + heavy PII (name/home-address/phone/email/Type). Target goumlyne (donations/donation_categories/persons — lane maps). CAI-474 conditions: idempotent+dedup+assert-empty, PII-minimize + RLS-no-anon, money sum+per-Type reconcile additive-only, cc-reviewer money+PII pass, guarded --expect-ref goumlyne, anon-denied post-proof. AUTHORED+DRY-RUN only. NEXT: lane reports dry-run → I verify → cc-reviewer money+PII pass → send package to cai → cai re-verifies (sample+RLS) + flips grant → hub applies + post-proof. (tins file `Irsyad Classlist and Barcode.xls` = already-loaded roster, not needed.)
3. **C3 test batch: CLOSED.** DELETE-PROVENANCE-1 met by partner confirm operator_messages #5027 ("C3 batch is a test. It is safe to delete", attributable, 06:35Z) per op #5100. All 4 C3 artifacts (batch 8 + tin 1797 + student eb20d2a9 + person 142f2277) soft-deleted + reversible; stays deleted. Reported to cai (#9561). Every C3 mutation was gated through cai (469 pre-clear→470 bless) — done right.
4. **Non-money sandbox + azlia (CLEARED, orch executes+audits):** NOT started. (a) ceayj sandbox 515a862b: rename auth acct abdul.sukur@gazzabyte.sg→sales@gazzabyte.sg + preparer role + SYSTEM INVITE (not raw creds) + SYNTHETIC mock data only (NO real minors-PII into partner sandbox). (b) azlia@irsyad.edu.sg (Sr Finance = org_admin): create+invite on GOUMLYNE via system invite (her prior invite failed — goumlyne SMTP config unknown; use generateLink if email doesn't send). Flag Nazim when access model set. Note plan posted #9503.
5. **kk-drilldown bug fix:** committed fix/kk-class-completion-drilldown @1cacca7 (drill-down was hiding UNRETURNED students). Nazim GO to deploy (#9462, read-path, outside freeze) AFTER: my verify (re-run tests incl reconciliation) + cc-reviewer pass + provenance. NOT deployed yet.
6. **WP-C: LIVE** (14f5684). Migration 071 = CAI-RESP-467 pre-cleared, flips to granted on cai's window-close + final drift re-check — non-urgent, feature already live.

## Inbox state / infra
- Bus + operator_log CLEAN as of handoff. Lanes running: cc-ihsanos-mig107 (building). Others stood down.
- Gates PENDING cai: mig-107 grant (on package), donor-import method review (#9561), 071 window-close flip, C3 provenance-close (#9561).
- DSNs: goumlyne=GOUMLYNE_DATABASE_URL (ref goumlynecruxrlmzlntp), ceayj=IHSANOS_PROD_DATABASE_URL, substrate/bus=DATABASE_URL. ihsanos Vercel deploys under the **wingmen** team (memory ihsanos-vercel-team-project), NOT the .env team.
- Two Nazims were running today; consolidated to one (tmux). Continuity fine.
