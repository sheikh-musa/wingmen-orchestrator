# FULL audit — PR #408 letter: CAI-1234 (a) DARK zakat-allocation line + (b) receipt-# ref

**Auditor:** cc-quality (FULL, with orch-console opus) · **Date:** 2026-08-21 · **Verdict: PASS, merge-ready.**
Requested by orch-console (bus #30384, thread `6f456ec6`). Verified at source. Money/PII-adjacent (zakat + email_encrypted).

Pinned HEAD `22b29bbfaa8121a0422a5daa1b6a1e0aaca22977` (= `gh pr view 408`, MERGEABLE, base `main`).
5 files, +123, **no migration**. Gates: **lint:all EXIT 0** (16) · **vitest 24/24** (letter-data suite: new CAI-1234 + prior CAI-1229).

## Focus items — all confirmed at source
1. **DARK-GATE INTEGRITY.** `ZAKAT_ALLOCATION_LINE_LIVE = false as boolean`. `resolveZakatAllocationNotice(isZakat, flagLive) = isZakat && flagLive ? NOTICE : null`; `buildAppreciationLetterData` calls it as `resolveZakatAllocationNotice(args.isZakat ?? false, ZAKAT_ALLOCATION_LINE_LIVE)` — with the flag baked `false`, `zakatAllocationNotice` is **always null** in this PR, so the PDF's `{data.zakatAllocationNotice ? <Text> : null}` never renders. **Bypass grep:** the only consumers of `ZAKAT_ALLOCATION_NOTICE` / `resolveZakatAllocationNotice` / `zakatAllocationNotice` / `ZAKAT_ALLOCATION_LINE_LIVE` are this single builder→PDF chain (no code renders the notice constant directly). So the flag being `false` is the ONLY thing between dark and live; there is no other render path. Never appears on any live letter in this PR. Tested (flag-off, all 4 isZakat×flag combos → null, builder dark-by-default).
2. **WORDING — present-tense, single-source.** `ZAKAT_ALLOCATION_NOTICE = "Zakat contributions are channeled to 4 Asnaf categories: Miskin, Riqab, Amil, and Fisabilillah."` — present tense ("are channeled"), one editable constant. The literal appears in exactly ONE place (grep). Client-pending asnaf/tense can be set at flip time without a rebuild.
3. **RECEIPT-REF.** `receiptReference = args.receiptNumber ? \`Reference: Receipt No. ${args.receiptNumber}\` : null` — renders only when a number exists, null otherwise. Number is `formatReceiptNo(receipt)` (send path: `receiptNo`; PDF route: `formatReceiptNo(receipt)`), which yields `TX{yy}{seq}` when `receipt_year`+`receipt_seq` exist else `RCP-{number}`. The route correctly **adds `receipt_year, receipt_seq`** to its select so the letter's ref matches the receipt-PDF format (without them it would wrongly fall back to `RCP-`). Tested (renders correct number; omits on null).
4. **TAX NOTICE BYTE-UNTOUCHED.** `src/shared/lib/non-deductible-notice.ts` is **not in the diff** (5 files, none is it); the `nonDeductibleNotice` passthrough in both callers is unchanged. No drift on the CAI-1229 keep-approved notice.
5. **NO NEW PII ON THE LETTER + no RAIL 4 risk.** Precise fact: `email_encrypted` was **not newly added** by this PR — it was already in receipt-email.tsx's send-path select (unchanged); the PR's only select deltas are `category_type` (both files) + `receipt_year, receipt_seq` (route). `email_encrypted` is used strictly on the **send path** (`emailEncrypted → decryptPii(emailEncrypted) → donor.email` transport, receipt-email.tsx:97-99,252) and is **never** placed into `letterData` (the letter person is `{displayName, gender, address}` only; `AppreciationLetterData` has no email field). The **PDF route does not even select** `email_encrypted` (`persons!left(display_name, gender, address)`). So no email/PII reaches the letter/PDF. **No audit-write added** anywhere in the diff — the letter/PDF render path is a GET with no audit-write, and none is introduced (no RAIL 4 fail-open surface added).

## Verdict
**PASS.** The zakat line is genuinely dark (single flag gate, no bypass consumer, always-null this PR, tested), present-tense single-constant; receipt-ref conditional + correct-format + tested; the CAI-1229 tax notice is byte-untouched; email_encrypted stays send-path-only and never renders on the letter; no audit-write added (no RAIL 4 risk). Confirms orch-console's opus. Render stays DARK on deploy. Client+route only (no DB) → merges as code; no §6.6. Clear to merge on your chain.
