# FULL audit — PR #412 preparer-queue resilience (op#15106)

**Auditor:** cc-quality (FULL, no-self-merge check) · **Date:** 2026-08-21 · **Verdict: PASS, merge-ready.**
Requested by orch-console (bus #30563, thread `15d81712`). Real logic on the PII-adjacent donations path — verified at source + empirically (tests + mutation). Follow-up to #405 (the 414 fix).

Pinned HEAD `b84a0115d60d734f7cb072e5b5e8e2531c5a7bce` (= `gh pr view 412`, MERGEABLE, base `main`).
7 files, +243/-15, **no migration**. Gates: **lint:all EXIT 0** · **load-error + with-timeout 7/7** · **donations/dashboard regression sweep 22/22**.

## Confirmed empirically
1. **loadError → VISIBLE, RECOVERABLE state; never infinite skeleton, never misleading empty.** `donations-list.tsx` renders `loadError ? <AlertTriangle "We couldn't load your receipt queue" / "We couldn't load donations" + "Try again">` **before** the `donations.length === 0` empty branch, so the error card wins over the empty state. The Suspense skeleton (`donations/loading.tsx`) can no longer hang because the page now always resolves (see #2). New `donations-list-load-error.test.tsx` (3 cases): loadError+queue → queue error + retry + **NOT** "No donations yet"; loadError+default → generic error + retry; no-loadError+empty → ordinary empty, NOT the banner. **Green on head; mutation-proven:** stripping the `loadError` branch (simulating the pre-fix throw-through) turns the 2 loadError cases **RED** while the no-loadError case stays green — the test genuinely guards the fix.
2. **page.tsx truly CATCHES + passes loadError, does NOT swallow-to-empty; home hardening has no healthy-load regression.** `donations/page.tsx` wraps the `listDonations` promise in `.then(r => ({...r, loadError:false})).catch(e => { console.error(...); return {donations:[],count:0,loadError:true}; })` inside `Promise.all` — a throw no longer rejects the render; `loadError` is passed through. It deliberately does **not** return a bare empty (which would falsely read as "nothing needs a receipt" and hide the failure). Home (`dashboard/page.tsx`) wraps each of the 4 home actions + `getOrgModuleConfig` + the org-settings read in `withTimeout(..., {data:null,error:null}, label)` — a healthy load resolves to the real value (no behaviour change); only a genuinely-hung await degrades to a visible partial home instead of an eternal skeleton.
3. **with-timeout.ts correct.** `withTimeout(p, fallback, label, ms=8000)` resolves to `p`'s value if it settles first, else `fallback` on timeout OR rejection — **never rejects**; clears the timer on settle so a late fallback can't overwrite a real value. Its test (4 cases: real value / hung→fallback / reject→fallback / timer-cleared) passes.
4. **No data/PII display change; nav gate is real defense-in-depth.** The only render changes are the error card + timeout wrappers; no PII/donation field added or altered. The `Receipts` nav child gets `roles:["org_admin"]` (dashboard-shell.tsx) — and the nav render **enforces** child roles (`if (child.roles && !child.roles.includes(userRole)) return false`, line 261; `ModuleNavChild` carries `roles?`). This matches the page's existing gate (`/dashboard/receipts` `notFound()`s non-`org_admin`, receipts/page.tsx:29 [Satr]) — it hides a dead link for preparers/cashiers/viewers, **reducing** exposure, not adding any. No migration.

## Observation (non-blocking)
This is the **resilience class-fix** (make the failure visible + recoverable), not the root-cause. `page.tsx`'s own comment notes the specific preparer `listDonations` error is a "substrate/PostgREST-layer follow-up (routed to console)". So the preparer receipt-queue will render the error card (correct, visible-degraded — far better than the infinite skeleton) until that underlying throw is root-caused. Worth tracking the follow-up; ties to the #405 embed queue-lens work (e.g. verify it isn't the latent empty-userId uuid path or an embed edge). Does not block this PR — the resilience fix is correct regardless of cause.

## Verdict
**PASS.** loadError surfaces a visible recoverable state (mutation-proven), never an infinite skeleton or misleading empty; page.tsx catches without swallowing; home is timeout-bounded with no healthy-load regression; withTimeout is correct; nav gate is enforced and matches the page; no data/PII change, no migration. Client-only → merges as code; no §6.6. Routing to orch-console for merge (no self-merge). Track the routed root-cause follow-up.
