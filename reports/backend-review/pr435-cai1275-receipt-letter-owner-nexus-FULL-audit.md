# PR #435 — CAI-1275 preparer/cashier owner-nexus receipt+letter access — cc-quality FULL money/PII access-control audit

**VERDICT: PASS** (money+PII access-control, CAI-1170). Pure app-code, no migration. Access-control gates A-E all hold; mutation-proved. Two builder flags: both non-blocking (flag 1 is a pre-existing policy tension to escalate; flag 2 is a UX gap). I remain propose-only — console holds the access-control gate.
**Auditor:** cc-quality (opus-4-8, `.quality_model` carve-out) · **Date:** 2026-08-22 · **Head:** ab88d929 · **Dispatch:** cc-irsyad #31408 (P2).

## The nexus (receipt-permissions.ts) — clean pure comparator
`isReceiptOwnerNexus({role, callerId, donationCreatedBy})`: `if (role !== "preparer" && role !== "cashier") return false;` → `if (!donationCreatedBy) return false;` → `return donationCreatedBy === callerId;`. Per-instance grant (this donation only), never a role-wide widening. Evaluated as an ADDITIONAL OR-path alongside (never instead of) checkReceiptPermission.

## Gates A-E — all PASS
- **A (server-side + caller-bound):** both routes use `createServerClient()` (SESSION client → RLS applies; NOT service-role + app-filter). `callerId = ctx.user.id` server-resolved via resolveActiveOrgContext; direct `created_by === callerId` comparison; never client-supplied, never UI-only (the routes are the enforcement point).
- **B (fail-closed on error):** BOTH routes `const {data: receipt, error: receiptError} = await supabase…single(); if (receiptError || !receipt) { …; return 404 }` — the fetch error is explicitly checked (NOT discarded — the CAI-1222 trap is avoided) and denies with 404 BEFORE the permission decision. A null/unresolved donation → `donationCreatedBy` undefined → isReceiptOwnerNexus returns false (fail-closed). **Mutation-proved**: grant-all mutation → 4 tests red.
- **C (identity mapping):** verified at source — profiles.id `UUID PRIMARY KEY REFERENCES auth.users(id)` (mig001); donations.created_by `UUID NOT NULL REFERENCES profiles(id)` (mig002). So created_by and ctx.user.id (auth.uid) are the SAME identity space — the direct comparison is correct (no join, no mis-grant, no silent deny-all).
- **D (audit):** both routes fold `granted_via_owner_nexus` into the CAI-894 donor-PII-print audit payload (not a separate write); `grantedViaOwnerNexus = !mayPrintViaGrant && mayPrintViaOwnerNexus` (true only when access came solely via the nexus). Traceability correct.
- **E (list/browse unchanged):** receipts.ts / actions/receipts.ts NOT in the diff (empty diff — byte-identical). Only the 2 pdf routes + receipt-permissions.ts + tests touched. No list/search endpoint loosened.
- **Pre-permission fetch exposes nothing new:** the reordered fetch (donation.created_by now read before the print decision) uses the session client → RLS scopes receipts SELECT to org staff (`auth_user_staff_org_ids`, mig069/095); print was always an app-layer gate ON TOP of that RLS. So fetching first exposes nothing to a role RLS wouldn't already let read. Reasoning sound.
- **Additive OR-path:** checkReceiptPermission is called unchanged (same args); the nexus is a pure OR that grants MORE to a preparer/cashier who processed the donation, never less. org_admin blanket + genuinely-granted cashier/preparer unaffected. **Mutation-proved** the role gate too (drop → 1 test red).

## Gates (at ab88d929)
- tsc clean (changed files) · **lint:all EXIT 0** incl. **check-pii-read-role-source OK** (all decrypt-on-read callers resolve role server-side — directly relevant to this PII surface), schema-drift/action-error-capture/minors-exclusion 0-new · **vitest 28/28** (receipt-permissions + both owner-nexus route suites) · **mutation-proved** owner-binding + role-gate.

## Two builder flags — technical weigh-in (policy = console/cai)
1. **Block (500) on audit-write failure (op#26418) vs console condition D (audit best-effort, never block).** This is **PRE-EXISTING** (op#26418), NOT introduced by #435 — the builder correctly left the audit-failure handling untouched; #435 only adds the OR-path to the permission decision. **Technical read: block-on-audit-failure is CORRECT/fail-closed for a donor-PII EGRESS surface** — the audit is the accountability gate for serving PII, so it must fail closed (distinct from CAI-1250's best-effort-*logging*, which is for non-egress logging protected by an independent role gate; here the audit gates the egress itself, so "audit IS the gate → fail closed" applies). The condition-D-vs-op#26418 conflict is a pre-existing POLICY fork, not a #435 change — **I escalate the reconciliation to console/cai** (advisory); technical recommendation is keep the block for this PII-print surface. **Not a #435 blocker.**
2. **Cashier can't SEE the donations-list letter link (gated admin||preparer) though CAI-1275 names cashier.** The BACKEND nexus DOES grant cashier (isReceiptOwnerNexus allows role==='cashier') — **role-correct per CAI-1275**, verified. The list-UI omission is a UX gap (the just-issued screen works for all 3; cashier reaches the letter there), NOT an access violation. Recommend a UX follow-up to surface the link to cashier. Non-blocking.

**Bottom line: PASS. Access-control is server-side, caller-bound, fail-closed on error, identity-mapping-correct, audited, additive (list/browse untouched), and mutation-proved. Flag 1 = escalate the pre-existing block-vs-best-effort policy to console/cai (technically keep the block); flag 2 = cashier backend grant is role-correct, UI is a UX follow-up. Console holds the access-control gate.**

---
## ADDENDUM — console ruling on flag 1 (op#26418 fail-closed STANDS, condition D withdrawn) — verified
Per console #31411 (relayed cc-irsyad #31414): condition D (audit best-effort) is REPLACED by op#26418 audit-write FAIL-CLOSED for this donor-PII-print surface. Re-verified the fail-closed precondition holds genuinely end-to-end (a failed audit ⇒ request DENIED, never a served-but-unlogged print) on BOTH routes and ALL paths:
- **PDF route:** the audit block (lines ~145-316; view-as via write_audit_log_secure RPC + non-view-as via writeAuditLog + missing-session — EVERY failure branch returns `{status:500}`) executes BEFORE **both** PDF-serving returns: the cache-hit path (`if (receipt.pdf_path) … return new NextResponse(…application/pdf)`, line ~322-336) AND the render path (line ~433). No PDF-serving return exists before the audit block.
- **Letter route:** audit block (lines ~145-229, all failure branches → 500) precedes the sole serve (line ~293; letter renders fresh, no cache).
- Applies to the nexus path AND the existing granted-role/org_admin/view-as paths — they all flow through the same audit block before any serve.
⇒ **op#26418 fail-closed audit precondition is genuinely enforced** — no served-but-unlogged donor-PII print path exists. Flag 1 fully resolved (not a defect; verified correct). Flag 2: cashier backend nexus grant role-correct (verified); UI list-link = out-of-scope UX follow-on. **#435 PASS stands, strengthened.**
