# mig238 (tabung_report_attach_deposit → +subadmin, scope=umum backstop) — APPLIED both silos

**Applier:** orch-console (Nazim), opus-4-8. **Date:** 2026-08-23. **PR:** #483 (Piece-2, split per cai CAI-RESP-1310).
**Grant:** cai CAI-RESP-1311 (§6.6, money-path, both silos). **Classification:** CREATE OR REPLACE of one SECDEF fn, +subadmin, scope backstop.

## Grant chain (complete before apply)
- coord floor-PASS (32636).
- cc-storefront non-irsyad adversarial FULL verify PASS both silos (32647) — clean, only the 2 intended deltas vs live.
- My independent wet-prove PASS: goumlyne D1-D6 + ceayj D1-D6 (scope backstop subadmin umum-only/keluarga+both refused inside the SECDEF fn; non-member refused; preparer unchanged; write-once immutable).
- cai grant CAI-RESP-1311.

## Apply-time body-diff-vs-live (per [[feedback_wetprove_create_or_replace_diff_body_vs_live]])
Before applying, captured LIVE `pg_get_functiondef` on both silos and confirmed the proposed body preserves EVERY live control (deposit_reference_required, deposit_slip_required, report_not_found, actor_not_permitted_to_attach_deposit, report_not_open_for_deposit, tabung_deposit_reference_immutable) — NONE silently stripped — and adds only the 2 intended deltas (role +subadmin; subadmin_scope_not_permitted backstop). This is the check that caught mig237's regression; mig238 is clean.

## Apply
Direct psycopg, own BEGIN..COMMIT (CAI-756-stripped, not `\i`), both silos. Post-apply verified on FRESH connections: subadmin + scope backstop present AND all 6 controls retained = True on goumlyne AND ceayj.

## Follow-up
- **#483 PR merge WAITS on mig237's re-derive** (mig237 stripped the CAI-1107/1297 completeness guard; re-derive routed to cc6). mig238 applied now (migrate-before-deploy); the PR carries both migs + code, merges once mig237 is fixed + granted.
