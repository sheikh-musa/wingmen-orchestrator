# mig236 (createDonorInline subadmin widen) — APPLIED both silos

**Applier:** orch-console (Nazim), opus-4-8. **Date:** 2026-08-23. **PR:** #481 (`feat/subadmin-donor-inline-widen`).
**Classification:** OUTSIDE-§6.6 (additive RLS OR-branches, donor-scoped; no fund/ledger/balance, no new PII class) → console apply, no cai grant (CAI-RESP-1290).

## What it does
Three policy replacements (DROP+CREATE), the last [CLEARED] write surface:
1. `persons` INSERT — `+subadmin` OR-branch (old body already included org_admin/cashier/preparer; delta = subadmin only).
2. `person_roles` INSERT — `+subadmin` branch **scoped `WITH CHECK role_type='donor'`** (RLS is the control, not the app's hardcoded 'donor'; subadmin can attach a donor role and nothing else).
3. `person_roles` SELECT — `+subadmin` branch `role_type='donor' AND auth_user_subadmin_org_ids()`; required so `INSERT ... RETURNING`/`.select("id")` doesn't hard-fail (Postgres RLS+RETURNING). Existing staff (`role_type<>'beneficiary' AND staff`) + org_admin branches **byte-untouched**.

## Pre-apply verification at source
- Both silos **byte-identical** for all 3 policies pre-apply; all 3 DROP target names matched live policy names exactly (no silent-no-op/duplicate trap).
- Old `persons`/`person_roles` INSERT bodies already carried `preparer` → the only real delta is `+subadmin` (no unflagged widen).
- Referenced fns pre-exist (`auth_user_subadmin_org_ids` = `auth_user_org_ids_with_roles(['subadmin'])`, etc.); no new function created.

## Wet-prove (BEGIN..ROLLBACK, mig BEGIN/COMMIT stripped per CAI-756; simulated sessions via SET ROLE authenticated + request.jwt.claims; meaningful negatives with real ground-truth)
**goumlyne: 10/10 ALL PASS · ceayj: 10/10 ALL PASS**
- P1 subadmin CAN insert persons ✓ · P2 subadmin CAN insert person_roles(donor)+RETURNING ✓ (proves 3rd-policy SELECT-back fix)
- N1 subadmin CANNOT insert beneficiary ✓ · N2 CANNOT insert student ✓ (WITH CHECK enforced)
- N3 subadmin sees 0 beneficiary/student ✓ — MEANINGFUL (goumlyne: 2 test ground-truth inserted; ceayj: 15 pre-existing real + 2 test = 17, org_admin sees all, subadmin sees 0)
- P3 subadmin sees donor rows ✓ (goumlyne 212, ceayj 9) · N4 cross-org: subadmin sees 0 donor in OTHER org ✓
- S4 org_admin STILL sees beneficiary/student ✓ (existing branch untouched)

## Apply
Applied via direct psycopg (own BEGIN..COMMIT, not `\i`), both silos. Verified on FRESH connections post-commit: 3/3 new policies present, 0 old policies remaining (clean replace) on goumlyne AND ceayj.

## Follow-ups
- **#481 merge**: gated on CI-green (studio-runner-starved) — merge = code+provenance only, migs already applied (migrate-before-deploy satisfied).
- **`_reserved.txt`**: does NOT record 236 (PR doesn't touch it). Collision-prevention holds via the gh-pr-list next-free check while #481 is open (claims 236). Coord to add `236=createDonorInline` + ensure Piece-2 (placeholder 236/237) renumbers to 237/238.
