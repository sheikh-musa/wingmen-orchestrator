# Orchestrator handoff — 2026-07-15 ~15:05Z (2nd reset today)

Written for a Nazim-greenlit checkpoint/reset (#8913) — I self-flagged marathon-session length before the from-scratch cc-support design doc (doctrine working: this morning silent-degraded 2h + operator-caught; now self-flag + self-recommend). Consensus=authority, operator informed. Everything below is also in the bus (`agent_messages`), `operator_messages`, and memory. Standing doctrine unchanged: money/PII/residency/definer steps stay cai/flag-gated; lanes build authored-unapplied, hub reviews+gates+applies; never `supabase db push` vs prod — direct-psycopg `--expect-ref`; irsyad-first smoke on shared deploys; verify, don't assert.

## JOB ONE (fresh-me, in order)
1. **P0 — apply migration 105 to ceayj (cai CAI-RESP-458 APPLY-GREEN, EXPEDITED).** ceayj `donation_categories` is LIVE-BROKEN: it has only `_deprecated_fund_raised`/`_deprecated_fund_target` while the pooled app reads/writes `fund_raised`/`fund_target` (which goumlyne correctly has). 105 = guarded, data-preserving, idempotent RENAME back to `fund_raised`/`fund_target`, `--expect-ref ceayjeamtmcyzzvqflus`, post-proof (50 rows intact + donations path works). **cc-infra authored it (authored-unapplied) + is holding; it will reply to #8927 with the exact staged paths (105 file + apply_*.py + purge script).** HUB runs the gated apply (dry-run → apply → proof), same pattern as the shipforge `apply_11_get_site.py` I ran earlier (ceayj conn = `IHSANOS_PROD_DATABASE_URL`, contains the ref). Been broken since ~07-08 (not Hadi-blocking — he took a real order 07-14 — but real; cai owns the CAI-456 "harmless" mis-call). **THEN** 105-apply's sibling:
2. **Purge the 14a55c8f test stub from ceayj** (cai GO, operator FYI already sent). It's a verified TEST STUB ('Madrasah Irsyad' on ceayj, created 07-08, 2 no-email persons/1 $0 tin/4 members — DISTINCT from the real irsyad org which lives on goumlyne). cc-infra authored the purge (verify NO real PII first → if any real PII STOP+migrate). Audit via a substrate record (NOT the hash-chain audit_log). HUB gates the delete.
3. **cc-support v1 full spec + boot recipe** (op#4443/4493, Nazim steer #8899/#8913 = highest-leverage, operator wants LOOK). This is why we reset — do it FRESH. A dedicated support agent that OWNS the irsyad/Gazzabyte group: reads inbound, answers DIRECTLY with grounded product knowledge, escalates UP only for money/PII/residency/builds/real-bugs (via attributable bus rows, not by making the hub type). HARD gates: money/PII/residency inform-not-execute (hub/cai-gated); distinct bot identity+channel (confirm); ground in ACTUAL live product state; escalate-not-guess below high-confidence; SUPERVISED BREAK-IN (it drafts → hub approves → then unsupervised); every reply logged. Write it → post to Nazim → he relays to operator for LOOK before spinning (Syed's token). v1 spec I floated is in bus #8840.

## DONE this session (all verified)
- **Elly empty-tin fix LIVE** (398f310) + **class-level Tabung report LIVE** (57a7818, PR#161, at /dashboard/tabung/keluarga) + **payment-confirm fail-closed fix LIVE** (078f3065, PR#162 — closed the silent-RLS "reports paid when DB unchanged" risk on Hadi's live path; cc-reviewer money-gate APPROVE; logged to work_outputs job 204/wo 278). All 3 gated: preview-green → FF main → both prod deploys READY → route-healthy. Confirmed to the Gazzabyte team (they relay to Elly; address the TEAM not "Elly", op#4432).
- **Shipforge 11_ get_site APPLIED to ceayj + all 3 CAI-454 proofs GREEN** → handed Nazim step-2 (synthetic copy of 7 substrate shipforge tables → ceayj; then I originate the SHIPFORGE_DB_URL flip — NOT before his copy; then 30_ decommission). His ~9% budget sets timing. Bus #8860.
- **Metered-API leak fixed fleet-wide + PERMANENT.** Root cause = tmux SERVER-global env overriding launcher shell-unset (see [[metered-api-tmux-server-global]]). Fixed boot_orch.sh (e338b46, 5996a25) + lanes.sh + boot_cai.sh (541e2b1); verified each launcher (op#4449). Swept fleet: stopped 3 idle metered legacy lanes → fleet 100% on Max. **VERIFY billing via `ps eww <pane_pid>` (ANTHROPIC_API_KEY absent/empty=Max), never the splash.**
- **Gazzabyte test account** — a live-goumlyne provisioning NEAR-MISS (a HOLD lost the race, org_admin+invite briefly created on the REAL irsyad org) caught, torn down + independently verified clean; then a residency-clean SYNTHETIC sandbox built (ceayj org **515a862b**, mock data, Abdul=org_admin isolated to it) + hub-verified + **login relayed to Abdul**. Teardown after test period: all rows carry org_id 515a862b + Abdul uid c1bafafe (see #8878). Lesson: [[settle-architecture-before-live-provisioning]].
- **Money-drift triage** (cai CAI-RESP-456→458): goumlyne CORRECT, ceayj was the anomaly → job-one #1/#2 above.
- Bus reconciled from 519 stale → single digits. Console white-screen was operator-not-on-Tailscale (server healthy fc-v10). Answered the operator on the bus queues (agent_messages hub inbox vs console /api/queue lane work-queue).

## PENDING (others / deferred)
- **Nazim: shipforge step-2** synthetic copy → then I originate the SHIPFORGE_DB_URL flip.
- **cai CAI-RESP-457 (UAE residency)** = next-market guidance (managed Postgres in UAE region, not Supabase; shipforge scoped-role pattern). NOT provision-now; shapes scope. FYI.
- **Client feedback awaited:** Abdul on the sandbox, Elly on the class-report.
- **Deferred P2 (behind job-one):** console views — token/auth Max-vs-metered indicator (#8910 op#4505) + whose-token primary (#8917 op#4510) + context-bloat % (#8810) + bus-queue-drain (#8879) → cc-console; context-health watchdog (#8807); backlog stamp-on-reply root-fix (#8881); task#61 report#57 trial-tin exclusion (#8659, gated money-correction on goumlyne, review→cai); Tarbiyah lane spec (#8763, Nazim owns scope). Queue-steer (Nazim #8899): cc-support + console-views ahead of backlog/watchdog.

## INFRA DEBT
- `restart_orch.sh` has stale `/Users/sheikhmusa` paths + wrong default label (`dev.wingmen.orchestrator` vs actual `dev.wingmen.cc-orch`). Reset the hub via `tmux kill-session -t orch` (KeepAlive respawns boot_orch.sh → `claude --continue` on Max). `fleet_lanes.worktree_path` for storefront also stale-sheikhmusa.
- ceayj admin conn = `IHSANOS_PROD_DATABASE_URL` (.env); goumlyne = `GOUMLYNE_DATABASE_URL`; substrate/orch bus = `DATABASE_URL`.
