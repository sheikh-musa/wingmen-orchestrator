# Nazim handoff — 2026-08-02 (REVENUE-PHASE recycle, op#9250)

_You are **Nazim / console body** (Mac Mini, `ORCH_BODY_ROLE=console`, tmux `nazim`). Reply to the operator ONLY via `scripts/nazim_send.sh` (a terminal reply does NOT reach his phone); photos via `scripts/nazim_send_photo.sh`. EACH TURN reconcile BOTH `operator_log.unprocessed()` AND `agent_messages` to `orch-console`; answer, then stamp handled. Reach agents via attributable `agent_messages` rows (from_agent='orch-console'); NEVER the hub's `tg_send`. Env: `set -a; . ./.env; set +a` then `.venv/bin/python3` (psycopg); substrate DSN = `DATABASE_URL`._

## ⚑ FINAL STATE (read FIRST; supersedes everything below)
Ran a very long, very productive session (op#9112→9250). **We moved into REVENUE + a BUILD PHASE.** Prior-me recycled at ~70% context deliberately, so a fresh-you sequences the build at full capacity. **The revenue strategy is LOCKED; the build is GREENLIT — your job now is to SEQUENCE + DRIVE the build (delegate to lanes), keeping money cai-gated.**

- **cc-finance (head-of-revenue) is LIVE** (Mini tmux `finance`, base `cc-finance`, Musa token + Opus 4.8; charter `~/wingmen/projects/finance/CHARTER.md`; its own operator channel **@wingmen_revenue_bot / finance-console**; voice `scripts/finance_send.sh`; realtime notifier `finance_bus_notify` live). **It owns the finance-console operator thread — ONE money voice to the operator.** It ran tonight's whole pricing + Gazzabyte economics with the operator, keeps money gated (proposes → operator decides → cai gates wiring), and is sharp + honest. Coordinate it via the bus; it runs the Gazzabyte split past you before the operator. It created `revenue_ledger` + stewards `finance_subscriptions`/`finance_burn`.
- **PRICING APPROVED** (op#9189): Shipforge free-preview / S$149 launch / S$29-mo care / US$29 style add-on; Storefront flat-SaaS ~S$29-49/mo food (take-rate kept for masjid/nonprofit; 2.5% spec dropped).
- **GAZZABYTE PARTNERSHIP POLICY LOCKED** (operator-ratified; doc `projects/finance/policies/gazzabyte-partnership.md`, gitignored). Gazzabyte = **Shuq+Wan** (real partners, client/sales/relationship/support); Wingmen = build/hosting/platform/SLA/feature-builds. Standing split: one-time (~S$28k Irsyad-scale) → Gazzabyte; recurring (~S$6k/yr) → Wingmen (the sustaining annuity); +~S$2-4k deploy fee/future client. **Rule LOCKED: new module = new quote.** **IRSYAD = OUR FIRST REAL REVENUE** (~S$6k/yr recurring starts Y1 NOW, ~S$5.5k net covers fleet burn; Shuq+Wan keep ~S$28k one-time). Nothing books until collected + SoW signed + 38.2k reconciled; every payout cai-gated.

## 🟢 BUILD PHASE — GREENLIT, SEQUENCE THESE (money-wiring cai-gated, each)
Ship-list is on the operator's backlog as 💰 SHIP cards, each with a `done_when` (readiness = wired + can charge, NOT first real customer). Delegate builds to lanes; you coordinate.
- **Payment rail (backlog 39) — GREENLIT (op#9250):** rails-agnostic **payment-adapter layer** + **HitPay adapter FIRST** (SG, unblocks Irsyad revenue), then Stripe (Wingmen HAS a Stripe acct) + Xendit (SEA). **Irsyad needs eNETS** (school fees) — online eNETS not on HitPay/Stripe/Xendit per cc-finance; likely a **NETS eNETS-Gateway adapter** in the layer (cc-finance chasing HitPay-online-eNETS + PayNow/GIRO fallback). HitPay acct = operator to create. Adapter scaffolding + HitPay can build; **live money-take is cai-gated.**
- **grantEntitlements silent-fail fix (backlog 40):** NON-NEGOTIABLE pre-charge (a paid customer could silently get no access). Buildable NOW, no money-gate. Shipforge (`grantEntitlements()` ignores the Supabase POST result). The obvious FIRST build.
- **Support agent (backlog 45) — GO (op#9250):** client-facing support agent for irsyad end-users (augments Shuq+Wan). **GROUND it in the gazzabyte group context (channel `gazzabyte-irsyad`) as training data.** Reuse the **Ray-AI (AI Course-Administrator)** pattern. 2 roles (builder cc-irsyad exists + support new) — start as one. Monetizable beyond-DMS (new quote + recurring bump). Post-first-revenue account-growth. **Needs a real spec first** (roles/pricing-with-cc-finance/timing).
- Also: Shipforge catalog SKUs + S$ currency (42); prod onboarding + watermark + paid Vercel (43); Storefront flat-SaaS billing (44).

## 🔴 OPEN LOOPS / OPERATOR'S COURT
- Ship-list PRIORITIZATION (he swipes to order; that IS your worklist). HitPay account creation. SoW reconcile (34k vs 38.2k). Payment consolidation → Wingmen business account (cc-finance holding). Billing read-tokens for cc-finance (relayed: Anthropic Console / Vercel / Supabase / GCP / DO / registrar). SRE post-demo ARM signal. HeartMuLa Abu Dhabi desktop. Bayan bot. Token remediation.

## ✅ INFRA BUILT TONIGHT (all live, verified)
- **Console fc-v20:** peek fixed on ALL 3 coordinators (root-caused cai double-render collision — coords excluded from lanes list); reset fixed (iOS-PWA `window.confirm` → in-DOM two-tap); **swipe** on your-asks (→ prioritise / ← drop, instant push-nudge to me); **done-goals** (`operator_backlog.done_when`/`done_signal` + `scripts/backlog_done_checker.py` launchd auto-completes on hard evidence — deploy-live/PR-merged); removed irrelevant "Fleet is handling" section + fixed pulse count.
- **3 realtime bus-notifiers** (launchd, KeepAlive): `nazim_bus_notify` (existing) + NEW `cai_bus_notify` (via nudge_cai.sh) + NEW `finance_bus_notify` (via lane_nudge.sh) — poke each body the instant a bus row lands (closes the cai overnight-stall root cause). Generalize-to-all-lanes OFFERED, not built (his call).
- **2 VPS systemd publishers** (run as user `wingmen` on 91.107.235.77): `wingmen-coordinator-publisher` (hub live PANE → `coordinator_panes`, for peek) + NEW `wingmen-hub-ctx-publish` (hub context-bloat % → `cc_session_costs`, script `scripts/hub_ctx_publish.py` — the hub was on the VPS so its ctx% read stale-0; now all 3 coords show ctx%).
- **SRE lanes-only auto-recycle:** BUILT + DISARMED + cai-ACCEPTED (CAI-681, on-host verified). `fleet_health_boundaries.assert_sre_lane_red_permitted` + `scripts/sre_lane_recycle.py`. Singletons hard-unreachable; 3 mechanical fail-closed gates; audit-before-clear; disarmed. Pre-arm checklist: `reports/sre-lane-recycle-PRE-ARM-checklist-20260802.md` (fleet_lanes registry needs populating at arm time). 35 pytest pass.
- **mig134** tin-edit money-path: landed (cai granted CAI-680, hub applied, verified live goumlyne).

## LANE ROSTER (Mini, /usr/local/bin/tmux; all Musa fp + opus-4-8)
scholar, ihsanos-qa, caai, adcda, exams, cosem-port, storefront, cosem-video, irsyad, ihsanos-platform, **finance (cc-finance, NEW)**. Singletons: cai (Mini, has realtime notifier now), cc-fleet-health (SRE, Mini), hub (cc-orchestrator, VPS — now shows live pane + ctx% on console). cc-shipforge lane = down (spin up for the shipforge builds).

## STANDING RULES / LESSONS (tonight)
- **Money is cc-finance's lane, gated.** cc-finance owns the finance-console thread (ONE money voice to the operator). The HUB was told to stay out of finance (op#9224 — it overstepped asking the operator hosting-pricing; it acked + retracted). If a build needs a pricing input, route to cc-finance, not the operator.
- **done-goals = READINESS** (wired + can charge, verified by a test/sandbox charge), NOT first real customer (op#9206).
- **Context% is now a real gauge** (last-turn tokens / window). Don't cry "context-heavy" without checking it — prior-me was called out TWICE for a false fatigue-framing at 34% [[feedback_no_false_context_heavy_claim]]. The 70% recycle here is REAL (verified on the gauge). فإذا فرغت فانصب — when done, straight to the next thing; no manufactured pauses.
- Verify-not-assert every "done"; SRE auto-recovers routine lane wedges (verify money-adjacent ones); a name is not an implementation.
