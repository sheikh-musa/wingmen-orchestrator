# cc-substrate handoff (updated 2026-09-24 ~06:35Z, bus #42854→#42857)

## STATUS (2026-09-24, later): safe-prep (a)-(d) on the 5 blockers DONE, posted #42857 — awaiting orch-console decisions, (e) Hermes untouched

Orch-console's #42854 asked for 4 non-destructive prep items following the inventory below. All done, full detail in `reports/wingmen-core-drain-cutover-plan-op20655.md` (both copies, synced, new sections: "COSEM-PLATFORM RUNNER MOVE PLAN", "RUNNER-RESTART ATTRIBUTION", "CREDENTIAL FINGERPRINT CHECK").

- **(a) Uncommitted wingmen-core work: CAPTURED.** New branch `wingmen-core-leftovers-20260924` @ `081b5b7` on wingmen-core's OWN checkout (a separate git history from this worktree — that checkout is 502 commits behind origin, didn't touch that drift), pushed + verified via `git ls-remote`. Excluded: 6 `.orch_default_token*` backups (listed only) and `scratchpad/` (hundreds of old session files, not source work). orch-console triages live-vs-stale from here — don't merge it yourself.
- **(b) Credential fingerprints: mostly clean, one real anomaly.** cosem-sa.json/musa-oauth-token/syed-oauth-token all MATCH the Mini. `musa2-oauth-token` exists on BOTH hosts but with DIFFERENT values — not simple match/missing, needs a call on which is authoritative before anyone relies on either. `/etc/gzb-vpn.conf` confirmed MISSING everywhere else (not on Mini, not in the vault — only `gzb_sudo_password` is vaulted). Nothing copied/vaulted, report-only per instruction.
- **(c) Cosem runner move plan: WRITTEN, not executed.** Target = the Mini (residency, not gzb). Good news found: cosem-platform's one workflow needs only `[self-hosted, studio]`, no Playwright/Linux requirement — a genuine macOS runner satisfies it honestly, no stand-in-label trick needed like ihsanos. Mirrors the Mini's existing `mini-ci-runner` LaunchDaemon pattern exactly (full plist in the plan doc). Awaiting orch-console's ok before registering anything.
- **(d) Runner-restart attribution: best-evidenced, not certain.** Most likely orch-console itself, restarting `wingmen-core-runner` ~88 seconds after my own #41939 verdict surfaced the "CI throughput halved" caveat — timing rules out my own fork (already concluded by then), and the SSH evidence (this Mac's own IP + the shared `wingmen_vps` key) can't distinguish between bodies running on this machine beyond that correlation. Not stopped — identification only, per instruction. Whether to keep dual-runner is still an open call.
- **(e) Hermes Agent: NOT touched, NOT investigated further** — hands-off entirely until Musa answers, per explicit instruction.

**Nothing further for this lane until orch-console/Musa act on: the cosem move plan (c), which musa2-oauth-token value is authoritative (b), the dual-runner-or-not call (d), and Hermes awareness (from the original inventory).**

---


## STATUS (2026-09-24): Musa asked directly whether wingmen-core can power off (op#22237) — final inventory says NOT a clean YES, real BLOCKERs found

**Track A — wingmen-core final power-off inventory, posted #42853 (BLOCKER, supersedes the "greenlight is orch-console's call" line below — do NOT relay a clean YES off the 09-21 #41939 verdict alone):** read-only inventory of wingmen-core surfaced material drift since 09-21 plus several items never in the original 6-item plan scope at all. Full table in `reports/wingmen-core-drain-cutover-plan-op20655.md` under "FINAL POWER-OFF INVENTORY (2026-09-24)" (both copies, synced).

**Real BLOCKERs found:**
1. **`wingmen-core-runner` is running again right now**, actively executing real jobs — it was stopped by orch-console 09-21 ~07:04 UTC, then came back online at 07:52:03 UTC the SAME DAY (2 min after a root SSH login matching the session that ran the gzb-only test), never recorded anywhere. Current state contradicts the "stays stopped, 1-command rollback" plan — powering off today would kill an in-flight job.
2. **cosem-platform's GH Actions runner was NEVER migrated** — still on wingmen-core, untouched all session, client-facing (ADCDA). Item 2 of the plan was always in-scope but never executed.
3. **Hermes Agent** (`/root/.hermes/`) — a completely separate, actively-used agent system under root (own SOUL.md/kanban.db/sessions, state files modified as recently as today) that nothing in this entire effort, the plan doc, or any bus message ever accounted for. Almost certainly Musa's own separate tool. Not classified unilaterally — needs his explicit awareness.
4. **Real uncommitted git work** on wingmen-core's orchestrator checkout (modified `agent_wake.py`, fleet console static assets, `boot_orch.sh`/`reset_orch.sh`, a staged new file, several token backups) — would be lost on power-off, not captured elsewhere as far as checked.
5. **Two credential locations not confirmed duplicated elsewhere**: `/root/.wingmen/keys/` + `/home/wingmen/.wingmen/keys/` (oauth tokens, a cosem GCP service-account key), and `/etc/gzb-vpn.conf` (VPN portal creds — never onboarded into the vault, only the gzb sudo password was).

**Non-blockers/clean:** item 3 (console+ngrok) moved+verified on the Mini, wingmen-core's Python service just needs a trivial final stop; item 6 (cron) confirmed genuinely clean; ingest/tg-out/agent-wake-subscriber confirmed inactive on wingmen-core (assumed fleet-owned on gzb per standing doctrine, NOT independently re-verified against gzb in this pass — flagged as a gap, not asserted). Items 4/5 publishers are redundant (running on both wingmen-core AND gzb, duplicate but harmless idempotent writes) — loop never closed.

**Nothing further to do on Track A until orch-console/Musa decide on the blockers above** — especially item 1 (who restarted the runner and why) and item 3 (Hermes Agent awareness).

---

# cc-substrate handoff (updated 2026-09-21 ~07:50Z, bus #41919→#41939)

## STATUS (2026-09-21 ~07:50Z): wingmen-core-runner STOPPED by orch-console, gzb-only re-test = ALL-ON-GZB NO TIMEOUT (+ throughput caveat) — verdict posted #41939, orch-console's greenlight call

**Track A — wingmen-core cutover, gzb-only load re-test done, posted #41939 (supersedes the NOT-DRAINABLE below, which was the dual-runner round-robin state):** orch-console stopped `wingmen-core-runner` (`systemctl stop` on wingmen-core, reversible — 1-command re-registration) after PR#751's in-flight CI finished, forcing all ihsanos studio-labeled CI onto `gzb-ihsanos-runner` by construction (confirmed live via `gh api actions/runners` — wingmen-core-runner offline the entire test window). Re-ran all 5 ihsanos workflows fresh. **Result: all 4 gating workflows (deploy-smoke, irsyad-frs-e2e, shop-synthtest-gate, tabung-correctness-gate) green**, individual exec durations 20s–6m56s, all within baseline, none timed out. `tabung-synthtest-gate` failed again (non-gating, same pre-existing chronic issue #40987, unrelated to this cutover). `runner_name` confirmed via ground truth on gzb itself (`journalctl -u ihsanos-runner` — GH's job API again showed the known `runner_name:null` propagation lag, not trusted) — every job name appears in gzb's own runner journal, and wingmen-core-runner was offline so there's no other candidate.

**One honest caveat surfaced, not part of orch-console's literal timeout criterion but flagged for the greenlight decision:** end-to-end wall-clock (trigger→completion) for the later-queued jobs was 20–35min (vs near-immediate with 2 runners) — NOT resource contention (`uptime` showed load avg 0.27–1.66 during the test, well below the plan's original 11-24 baseline figure) but structural: one GH Actions runner = one job at a time, and gzb-ihsanos-runner was also absorbing real concurrent CI from other in-flight ihsanos PRs during the window. Net: CI throughput is halved with wingmen-core-runner gone — real PRs will see slower feedback whenever volume overlaps, even though nothing actually times out. Quality/velocity tradeoff, not a correctness blocker.

**VERDICT posted: (a) ALL-ON-GZB, NO TIMEOUT** (bus #41939, thread on #41921/#41919/#41912). Full table + evidence in `reports/wingmen-core-drain-cutover-plan-op20655.md` under "2026-09-21 (later) — gzb-only load re-test" (both copies, synced). VPN raised for ground-truth checks, torn down cleanly afterward (default route confirmed intact). **The binary criterion clears — greenlighting Musa is orch-console's call from here, the throughput caveat is judgment not blocker.** Nothing further to do on Track A until orch-console decides.

---

## Prior update (2026-09-21 ~07:02Z, bus #41912→#41918) — SUPERSEDED by above, kept for the round-robin evidence trail

**STATUS at the time:** wingmen-core cutover Part A APPLIED by orch-console, final verification = NOT-DRAINABLE (2 real gaps) — vault Phase 1 fully closed, nits fixed

**Track A — wingmen-core cutover, final verification done, posted #41918:** orch-console applied Part A for real on gzb (git pull, 6 Chromium deps, 3 systemd units, runner relabeled `self-hosted,studio,Linux,X64,vps-fleet`) — see prior update below for that message's detail. I then ran the actual functional verification orch-console asked for (not process-active alone): re-ran all 5 ihsanos workflows for real. **Result: only 2/5 (irsyad-frs-e2e, tabung-correctness-gate) landed on `gzb-ihsanos-runner`.** The other 3 landed on `wingmen-core-runner` — deploy-smoke (success), shop-synthtest-gate (success), **tabung-synthtest-gate (FAILURE**, pre-existing chronic-red per #40987, cross-checked against 3 prior real runs, not a cutover regression, but still an open gap). Both `gzb-ihsanos-runner` AND `wingmen-core-runner` were observed **simultaneously busy** serving real unrelated production PR jobs during the test — concrete live proof wingmen-core-runner is still carrying real CI load right now, not safe to kill. Both runners share identical labels so GitHub round-robins between them; this is architecturally expected, not bad luck. DB freshness: `coordinator_panes` confirmed live (fresh row, publisher active on gzb); `cc_session_costs` publisher confirmed genuinely alive via direct `systemctl`+journal on gzb (writing every ~60s) but the tracked value hasn't moved in the window checked because gzb's hub session genuinely had no new turns — not a red flag, just not a "value-in-motion" proof point either.

**VERDICT posted: NOT-DRAINABLE** (bus #41918, thread on #41912). Full per-workflow table + evidence in `reports/wingmen-core-drain-cutover-plan-op20655.md` under "FINAL DRAINABLE VERIFICATION (2026-09-21)" (both copies, synced). **Two paths forward, orch-console's call, laid out in #41918:** (a) stop/disable `wingmen-core-runner`'s GitHub registration first so all 5 workflows are forced onto gzb by construction, then re-verify gzb alone handles full load without timeout (the plan's own risk section already flagged gzb's high baseline load as a real contention risk); or (b) accept dual-runner operation as the real end state and drop "fully replaced" as the bar. `tabung-synthtest-gate`'s chronic failure needs its own fix regardless of which path — unrelated to the cutover. **Nothing further to do on Track A until orch-console picks a direction.**

**Track B — fleet secrets vault (op#21338) — FULLY CLOSED this round.** Phase 1 gate-passed (#41841), Mini KEK bootstrapped by orch-console + independently wet-proven end-to-end on the real Keychain key (#41846). Two non-blocking Phase-2 nits from that gate pass were then fixed and verified: `put()`/`rotate()` now audit on failure too (parity with `get()`, confirmed via a real `KekNotFoundError` case landing a `success=False` row), and the test suite no longer fails confusingly in a bare shell missing `AGENT_ID`. Also found+fixed a real regression while verifying: the KEK-not-bootstrapped test depended on the Mini's Keychain staying empty, which broke the moment it got bootstrapped for real — re-pointed at a monkeypatched nonexistent host id so it still exercises the real macOS `security` failure path. 7/7 green in a bare shell, orch-console independently re-ran and confirmed (#41849). Committed `119b476`→pushed `2ed507c` on `fable/substrate-safe-fixes`. **Don't re-touch vault.py/test_vault.py or re-run the Keychain bootstrap** (would rotate the KEK). Next vault action is orch-console's: `vault.put("gzb_sudo_password", ..., leak_flagged=True)` the next time Musa sends it — already happened per Track A above (Part A used the vaulted password), so Phase 1's real-world proof case is effectively done too, just not narrated as a separate milestone.

---

## Prior update (2026-09-21 ~03:40Z, bus #41762…#41841)

**Track A — wingmen-core cutover:** unchanged from the prior update below EXCEPT the root-grant story changed twice more. (1) orch-console's #41813 claimed we already held a gazzai/gzb sudo password (op#19891) and told cc-substrate to just use it — a thorough sourced search (`.env`/`.secrets`/git/wingmen-core/gzb/DB history/local memory) found it was **never persisted, by design** (Musa: "nothing's on disk"; "I'm deliberately not pasting your sudo password into a logged transcript") and is flagged leak-exposed/rotation-pending — full trail in the plan doc's "CREDENTIAL SEARCH" section, posted as bus #41814, orch-console owned the over-read to Musa. (2) orch-console then (#41816) took the password-handling step itself — Musa re-sends it directly into orch-console's live session (never the bus), orch-console runs Part A over the VPN hop, loops cc-substrate back in for verification only (runner relabeled + green run on all 5 ihsanos workflows + fresh `coordinator_panes`/`cc_session_costs` rows from gzb). **Standing by in verify-mode — nothing to execute until orch-console pings that Part A landed.**

**Track B — fleet secrets vault (op#21338, Musa-greenlit) — Phase 1 BUILT (2026-09-21 ~03:40Z, bus #41841 gate-pass → build):** design doc `reports/fleet-secrets-vault-design-op21338.md` (both copies, synced) gate-passed by orch-console (#41841). Built and pushed this round:
- **Schema**: `migrations/065_secrets_vault.sql` applied to silo `tscuymavysscrvoberrr` (`vault_secrets` incl. reserved nullable `allowed_agents` for Phase 2+ ACL, `vault_secrets_history`, `vault_access_log`), RLS deny-all verified sufficient (anon/authenticated both `rolbypassrls=false`). One flagged tool gap, not a security gap: `apply_migration.py`'s REVOKE-requires-assert rule only supports function-privilege asserts, not table-level — documented in the migration + design doc addendum, did not fabricate a fake assert to dodge it.
- **Helper**: `nervous_system/vault.py` — `get`/`put`/`rotate`, AES-256-GCM envelope encryption (`cryptography`, no new dep), macOS Keychain KEK on the Mini / root-only-file KEK on Linux, gzb never issued one. `get()` returns a `VaultSecret` dataclass surfacing `leak_flagged` explicitly.
- **Tests**: `tests/test_vault.py`, 7/7 passing — pure crypto round-trip, the real (unmocked) KEK-not-bootstrapped failure path asserted to name the exact fix, a full put/get round trip + leak-flag/rotate via a monkeypatched KEK (isolates from real Keychain state, proves the whole pipeline).
- Committed `7d3578a` on `fable/substrate-safe-fixes` (`~/wingmen/orchestrator`; rebased through a concurrent push from the design-doc commit, one unrelated live-log file scope-stashed/restored cleanly per the standing stash rail). Bus reply posted to orch-console (thread #41827/#41829/#41838/#41841) with the exact Mini Keychain bootstrap command.

**Next action needed is NOT cc-substrate's — it's orch-console's Mini KEK bootstrap** (`security add-generic-password -s wingmen-vault-kek -a mini -w "$(openssl rand -base64 32)"`, bus #41841 note 1 — they run it themselves as sheikhmusa on the Mini). Once bootstrapped, the very next `vault.put("gzb_sudo_password", ..., leak_flagged=True, ...)` when Musa sends the password again is what closes Phase 1's proof case AND Track A's item 1/4/5 root-grant loop in one motion. **Phase 0 (read gzb's `fetch-secrets` unit) is still open** — two VPN-tunnel-up-but-inner-SSH-timeout attempts this session, retry when the tunnel's reachable; does not block anything currently waiting.

---

## Prior update (2026-09-20 ~17:39Z, bus #41762…#41809)
Latest this round (bus #41803→#41806, #41807→#41809): drafted + posted the copy-paste-ready gzb root/sudo access request for Musa to forward to Gazzabyte — least-privilege, two parts (Part A: one-time root session, 4 commands, ~5 min; Part B: optional standing NOPASSWD sudoers scoped to exactly the 3 new unit names, matching gazzai's existing whitelist pattern). While drafting the publisher half, found and fixed a REAL bug: `scripts/hub_ctx_publish.py` also hardcoded its `.env` load path (not just the project-dir lookup already known about) — both now derive from the script's own checkout location at runtime, tested against this Mac's real `~/.claude/projects/` mangled dirs. Committed `d803a32` on `fable/substrate-safe-fixes` in `~/wingmen/orchestrator`, pushed. **gzb's checkout needs `git pull` as step 0 of the root session** (confirmed via `gzb-vpn.sh up`, read-only check, torn down cleanly — currently at `8045851`, behind `d803a32`) or the old bug ships anyway. Full exact `ExecStart` lines for both publisher units (pulled verbatim from wingmen-core's live units via `systemctl cat`) are in the plan doc addendum and bus #41809 — ready to fold into the same root session as item 1's packet (#41806).

**Everything is now packaged.** Nothing left for this lane to do until the root grant lands (or Musa/orch-console says otherwise) — don't re-derive or re-send the packets, they're final and posted.

## Prior update (2026-09-20 ~14:40Z, bus #41762/#41763/#41768/#41777/#41778/#41782)
Musa wants wingmen-core gone; hub/bridge already cut over per orch-console. Ran the full verify→execute→verify cycle this session (bus #41762→#41763 verify, #41768→#41777 execute attempt, #41778→#41782 item-3 execution). **Item 3 (console+ngrok) is now DONE** — installed + fully verified live on the Mac Mini (this session runs physically ON the Mini, hostname `Sheikhs-Mini`, confirmed via `hostname`/`uname` — no SSH needed, contrary to earlier belief that this lane had no path to the Mini). **Items 1 + 4/5 remain blocked** on the same single root-on-gzb constraint identified 09-17 (also confirmed to block 4/5 — a no-root `systemctl --user` path doesn't work, `Linger=no` and enabling it needs root too). Full detail: `reports/wingmen-core-drain-cutover-plan-op20655.md`, "VERIFICATION ADDENDUM — item 3" (both copies, synced identical). wingmen-core's `wingmen-hosted-console.service` (Python process) and `wingmen-core-runner` are untouched/still running; ONLY `wingmen-console-ngrok.service` on wingmen-core was stopped (not disabled — one `systemctl start` away), and that was structurally required, not a shortcut — see below.

**Item 3 details, don't re-do:** `dev.wingmen.hosted-console` + `dev.wingmen.console-ngrok` launchd jobs installed (`~/Library/LaunchAgents/`, tracked at `launchd/dev.wingmen.{hosted-console,console-ngrok}.plist`), `CONSOLE_BEARER_TOKEN`+`NGROK_AUTHTOKEN` copied verbatim into this Mini's `~/wingmen/orchestrator/.env` from wingmen-core (not regenerated). Verified: local `:8788` and the public URL both return 200+real fleet data with the correct bearer, 401 with none/wrong, on both. **Important correction:** the plan's old "no reserved domain, URL will change" note was WRONG — ngrok's free authtoken has an account-level static domain (`monologue-riddance-sworn.ngrok-free.dev`) and only allows ONE concurrent session against it. Had to stop wingmen-core's ngrok tunnel (only the tunnel, not the console service) to prove the Mini's — the public URL is UNCHANGED (same domain, now served from the Mini), nothing to update in bookmarks/bot links. Structural consequence: wingmen-core's and the Mini's console tunnels can never both run again on this authtoken, cutover or not.

**Access note for gzb (items 1/4/5 only):** reaching gzb needs `sudo gzb-vpn.sh up` on hub-vps first (FortiGate split-tunnel, `/usr/local/sbin/gzb-vpn.sh`, 900s dead-man auto-teardown). First `up` attempt can fail transiently ("Peer refused to agree to our IP address" — stale IPCP lease) — retry once. `gazzai@<tailscale-ip>` is refused by design (LAN-only); always go via the `gzb` SSH alias after the tunnel is up. Tear down (`sudo gzb-vpn.sh down`) when done.

**Only one thing left unblocks the rest of this plan:** root/sudo grant on gzb for `gazzai` (unblocks items 1+4+5 in one pass — packets for all are already prepared/verified, just need applying). Nothing else for this lane to do until that lands.

---

## Prior update (2026-09-17 ~02:46Z, bus #40965/#40986/#40987/#40991)

## 1. Per-project governance build (op#20702) — CLOSED, all 3 merged
PR #112/#113/#114 all show `state: MERGED` on GitHub (verified `gh pr view` this session). No action needed here anymore — don't re-touch.

## 2. wingmen-core drain cutover (op#20655) — plan approved, PAUSED mid-execution (P1 governance work took priority)
Full plan: `reports/wingmen-core-drain-cutover-plan-op20655.md` (both this worktree and `~/wingmen/orchestrator/reports/` — reports/*.md is gitignored, copy to both if you ever update it). Nazim approved + cleared execution of items 1-6 in bus #40608.

**Done:**
- STEP 0 (durability blocker): `scripts/hub_ctx_publish.py` was untracked-only on wingmen-core — committed verbatim, PR #111, **merged** (trunk `7f1e1f4`).
- Decision 4: wingmen-core's `cc_session_costs` cron **removed** (crontab cleared) — it was NOT a no-op as first assumed; found and fixed a real bug (the shared `cc_session_costs_auto_writer.py` uses raw filesystem mtime as `ended_at`, not real conversation-turn timestamps — flagged to SRE for a fleet-wide check, not yet resolved elsewhere as far as you know). Stale `cc-orchestrator` row (id=2216) corrected to its true last-activity timestamp with a `notes` explanation.
- Decisions 2/3 accepted by Nazim: cosem runner replicates the exact label set `[self-hosted, studio, Linux, X64]` on gzb (a deliberate existing stand-in pattern, not a redesign); console+ngrok goes to **the Mini** (residency — a fleet-wide admin surface must not sit on Gazzabyte's own premises), no reserved ngrok domain so the URL will change on the move, capture + hand to Nazim when live.

**Item 1 — PARKED-WITH-A-PACKET per orch-console ruling #40987, resume here:** run #1 failed (Chromium deps missing on gzb, see below), orch-console ruled STOP the runner process (a runner that fails every job it grabs is worse than no runner) rather than re-try. Done this round: (a) `gzb-ihsanos-runner` (id 25) **stopped cleanly** — Ctrl-C on the `run.sh` tmux session, confirmed via `gh api actions/runners` → `status:offline, busy:false`; GitHub registration id 25 left in place (not `config.sh remove`'d) so re-enable is one command (`ssh hub-vps` → `sudo -u wingmen ssh gzb` → `cd /home/gazzai/actions-runner-ihsanos && ./run.sh`, or install the systemd unit below). `wingmen-core-runner` (id 23) untouched, still online. (b) The exact **needs-root packet** (verified apt package list for gzb's real Ubuntu 26.04 repos + full systemd unit draft) is written into `reports/wingmen-core-drain-cutover-plan-op20655.md` under item 1, both copies — that's the one-line ask Nazim carries to Musa/Gazzabyte. (c) Confirmed **only item 3 (console+ngrok) is Mini-side**; wrote the exact live unit configs from wingmen-core into the plan under item 3 as a ready handoff for Nazim (items 1/2 gzb-side root-blocked, items 4/5 gzb-side by design, item 6 closed). Bus thread `df84e497-...` (#40986→#40987→#40991) has the full back-and-forth. **Next move needs root on gzb — not actionable by me until that lands.** Don't re-run item 1's runner until then; a second failure on the same missing-deps cause adds no new signal.

**Original failure (for context, already resolved by the stop above):** first real job on the gzb runner (run 35173855616, job 105051114935, `tabung-synthtest`, PR#714) FAILED: `browserType.launch: Target page, context or browser has been closed` — gzb missing `libatk1.0-0t64`/`libatk-bridge2.0-0t64`/`libcups2t64`/`libxdamage1`/`libasound2t64`/`libatspi2.0-0t64` (verified via `apt-cache policy` against gzb's actual repos, `playwright install-deps --dry-run` itself refuses — Ubuntu 26.04 isn't yet in Playwright 1.59.1's supported-OS table). Separately confirmed (twice now — once on this failure, once again on the stop): GitHub's REST API (`actions/runners`, `actions/runs/.../jobs`) lags real gzb state by 20+ minutes before catching up — cross-check via SSH (tmux pane / runner diag log / process list) when it matters in the moment, don't trust the API's snapshot blindly on this host. Logged in the plan as observed, not built around.
- Item 1 (original scope, still true): labels `self-hosted, Linux, vps-fleet`, verify Chromium/Playwright system deps present, trigger a real CI run, confirm on the new runner by id (not just "it went green" — check `runner_name` in the job's API response — note the propagation-anomaly caveat above, may need to cross-check the runner's own diag log / tmux pane instead of trusting the API alone), keep the wingmen-core runner registered until 2-3 real green runs land on the new one.
- Item 2: same for the cosem-platform runner — **first read that repo's own `.github/workflows/*.yml`** (`~/wingmen/projects/cosem-platform`) to confirm its label requirements before assuming an identical drop-in (already partially checked: it requests `[self-hosted, studio]`, and wingmen-core's runner is deliberately double-labeled `studio`+`Linux`+`X64` as a stand-in for the real, currently-offline Mac Studio runner — replicate that exact label set on gzb, don't redesign it).
- Item 3: hosted console + ngrok → the Mini. Functional verify = curl the new tunnel URL with the correct bearer → 200 + real data; without/wrong bearer → 401.
- Items 4/5: the two publishers (`hub_ctx_publish.py`, `coordinator_pane_publisher.py`) → gzb, co-located with the live `orch` tmux session there (this is forced by co-location, not a real choice). `coordinator_pane_publisher.py` already exists in gzb's checkout; `hub_ctx_publish.py` needed a path fix for gzb's actual project-dir mangling (`-home-gazzai-wingmen-orchestrator`, not wingmen-core's `-home-wingmen-wingmen-orchestrator`) before it'll find anything there — do that fix as part of standing it up.
- Item 6: closed (cron removed, no re-home needed — the mtime bug meant there was nothing worth re-homing).
- **Access path**: `ssh hub-vps` (root, works directly) → `sudo -u wingmen ssh gzb '<cmd>'` (the `gzb` alias lives in wingmen-core's own `~/.ssh/config`, targets the private VPN IP as user `gazzai` — there is no direct path from the Mac Mini to gzb, must hop through wingmen-core). gzb is genuinely busy (load 11-24, 8 live tmux sessions: orch/irsyad-coord/irsyad-tabung-jumaat/irsyad-worker-1/bayanqa-bot/encoder/mizan-redirect/scholar) — it is Gazzabyte's own office server, not a spare wingmen VPS. Its orchestrator checkout has real uncommitted work (`nervous_system/ingest.py`, `scripts/irsyad_autoscaler.py`, `tests/test_irsyad_autoscaler.py` modified; `deploy/bayanqa_supervisor.sh`, `deploy/encoder_supervisor.sh` untracked) — **do not touch beyond adding new files/units**, Nazim is chasing the owner separately.
- Sequencing rule throughout: the gzb/Mini replacement serves/publishes and is functionally verified BEFORE the wingmen-core copy stops. Musa gives the final power-off word only after all 6 items reach "replacement proven."

## 3. ihsanos migration-tracker reconcile (op#20626) — FULLY DONE, one loose end
All 185 flagged migrations (original 31 + 154-file tail across 4 batches) resolved. PR #699 (`substrate-cleanup/migration-tracker-reconcile`, ihsanos repo) is **verified-clean but HELD** — not your fault, main CI is red on 2 pre-existing gates it inherits (migration-337 schema-drift + an ActionResult arch-test), routed to coord by Nazim (bus #40616). Merges automatically once main greens. **No action needed from you** unless Nazim says otherwise — don't re-touch this PR or re-run the reconcile.

Two live production defects (relink_donation, issue_receipts_for_donations) were found missing on ceayj despite being called from live code, and were live-applied there this session with a gated, smoke-tested apply (bus #40479 has the full transcript) — this is DONE, not pending.

## Standing rails (carried from prior handoffs, still true)
- Never `supabase db push`. `apply_migration.py --dry-run` / `--silo <ref>` is the sanctioned path for the orchestrator substrate; for ihsanos, `supabase db query --linked` (the CLI/Management-API path) since no write-capable direct DSN is provisioned here.
- Every DB write verified on a separate connection, never same-transaction visibility.
- `apply_migration.py --dry-run` does NOT surface PL/pgSQL `NOTICE` output — a real limitation, not just "trust the ok". Use the standalone verification-script pattern whenever you need to *prove* trigger/exception behavior, not just "no error was thrown."
- This worktree's path is `/Users/sheikhmusa/wingmen/projects/orchestrator.wt-cleanup`. The ihsanos reconcile work lives in the SEPARATE worktree `~/wingmen/projects/ihsanos-migtracker-reconcile`. Don't confuse the two; always `pwd` before an absolute-path Write.
- `git stash` is a SHARED stack across all worktrees/sessions — always `push -u -m "<tag>"`, capture the SHA immediately, restore via `apply <sha>` (not `pop`), re-find the current `stash@{n}` by tag before `drop`.
- Identity: verify `CC_BASE_AGENT_ID`/`CC_AGENT_ID` off your live pid every boot (should read `cc-substrate`).
- 058b and the 66-table default-DML sweep remain orch-console's to commission — untouched, unrelated to anything above.

## First actions on boot
1. Reconcile `agent_messages WHERE to_agent='cc-substrate' AND read_at IS NULL` — likely a root-access ruling from Nazim on item 1's needs-root packet, a resolution to the gzb-password gap below, or a new ask.
2. Verify identity (`CC_BASE_AGENT_ID`/`CC_AGENT_ID` off live pid — should read `cc-substrate`). Note: this worktree's `.env`/`.venv` are symlinks to `~/wingmen/orchestrator/{.env,.venv}` (added 2026-09-17, matches every sibling worktree's pattern — they were missing before, which blocked all DB access; if they're ever missing again, recreate the same way). This session/lane runs physically on the Mac Mini (`hostname` → `Sheikhs-Mini`) — no SSH needed for anything Mini-local (only gzb/wingmen-core need the SSH hop).
3. Governance workstream (op#20702) is CLOSED — don't re-touch #112/#113/#114.
4. **gzb sudo password: searched exhaustively 2026-09-20, genuinely not found anywhere retrievable** (see `reports/wingmen-core-drain-cutover-plan-op20655.md` "CREDENTIAL SEARCH" section for the full sourced trail — bus #41814). orch-console's #41813 claim that it was "stored" was checked and found incorrect; the password was hand-delivered by Musa into two past live sessions (09-12, 09-14) and deliberately never persisted. Don't re-search from scratch — read that section first. If root/sudo access on gzb has landed (via Musa resending the password live, or a Gazzabyte grant): apply the apt + systemd packet from the plan doc item 1 AND item 4/5's packet, restart the runner, re-verify a real CI run lands on it by `runner_name` (check all 5 label-dependent workflows, not just tabung-synthtest-gate — the runner needs labels `self-hosted,studio,Linux,X64,vps-fleet` to cover both label families), install the two publishers on gzb, then report "wingmen-core DRAINABLE" once all three are proven. If it hasn't landed: items 1/4/5 stay parked, nothing to do there.
5. Item 3 (console+ngrok) is DONE — don't re-do it. If anything looks wrong with it, check `launchctl print gui/$(id -u)/dev.wingmen.hosted-console` and `.../dev.wingmen.console-ngrok` first (both local to this machine).

## op#22237 execution round (2026-09-24, this fork)
- (c) cosem runner -> Mini: BLOCKED on interactive sudo (this Mac, not gzb). Plan ready, needs a human at the keyboard.
- (d) second gzb runner + drain wingmen-core ihsanos runner: DONE, verified. wingmen-core-runner deregistered+removed.
- (b) musa2-oauth mismatch resolved (wingmen-core's copy is current, Mini's is stale) + gzb-vpn.conf vaulted (byte-identical round-trip verified).
- (a) leftovers triage: only 2/26 genuinely not-in-trunk, neither urgent. Branch likely discardable.
- Reported: bus #42861 (thread on #42858).
- Remaining power-off blockers: (c)'s sudo gap, Hermes (hands-off, unresolved).
