# cc-substrate handoff (drain/re-token, op#12114 — 2026-09-16)

## PENDING WORK — ihsanos migration-tracker reconcile (op#20626), mid-flight
This is the active task. Nothing irreversible is stuck mid-transaction — every DB write this session was either fully committed+verified or fully rolled back+verified before moving on. Safe to resume from scratch.

**State:** Batches 1 and 2 of the tail (migrations #166-262, on top of the original 31 Nazim first listed) are DONE, reported, and merged into the working baseline. Batches 3 and 4 (#263-337, ~83 migrations) remain.

**Worktree:** `~/wingmen/projects/ihsanos-migtracker-reconcile` (a dedicated worktree off `origin/main@3789a41e`, NOT the shared main ihsanos checkout at `~/wingmen/projects/ihsanos` which has unrelated uncommitted work on `feat/irsyad-role-coverage` — do not touch that one). Branch `substrate-cleanup/migration-tracker-reconcile`, currently at `c59c5307`, pushed. PR: https://github.com/sheikh-musa/ihsanos/pull/699 (baseline.json + 3 migration-header edits, NOT the actual tracker bookkeeping — that's written directly to the DBs). Nazim said he'll merge PR#699 himself once CI (which runs this same lint) goes green across the full tail — do not merge it yourself.

### Resume steps
1. `cd ~/wingmen/projects/ihsanos-migtracker-reconcile && git status` — confirm still clean at c59c5307. `git fetch origin main` — confirm 3789a41e is still main's tip (Nazim may have pushed more since; if main moved, decide whether to rebase this worktree or just keep working off the same base — the reconcile target is whatever's actually in prod, not main's tip, so a stale base is fine for reading migration files, just re-`git fetch` before trusting `origin/main:<path>` blob shas for anything sensitive).
2. Reconcile `agent_messages WHERE to_agent='cc-substrate' AND read_at IS NULL` first — there may be a response to msg #40479 (my last report) or something else.
3. Get remaining drift: `supabase link --project-ref ceayjeamtmcyzzvqflus && node scripts/lint/check-migration-tracker-drift.mjs` (needs `SUPABASE_ACCESS_TOKEN` from `/Users/sheikhmusa/wingmen/orchestrator/.env`, sourced). Batch 3 = the next ~40 ascending from wherever it currently starts (was #263 as of drain time — re-derive, don't hardcode, in case batches 1-2's baseline needs a tweak).
4. Same method as batches 1-2 (see PR#699 commit history for the exact pattern): `/private/tmp/.../scratchpad/probe_migrations.py` (RO-probes both silos per migration's extracted objects — tables/columns/functions/policies/indexes/triggers/constraints/storage buckets) → manually resolve anything PARTIAL / NO-OBJECTS-EXTRACTED / ambiguous (check `docs/irsyad-requirements-register.md` and each file's own RESIDENCY line first) → record tracker rows only where genuinely applied, on the correct silo(s), via `supabase db query --linked --file <sql>` with `created_by='migration-tracker-reconcile'` → **always verify every write on a SEPARATE RO connection after** (`IHSANOS_PROD_RO_DATABASE_URL` / `GOUMLYNE_RO_DATABASE_URL` in the orchestrator `.env`) → baseline anything genuinely N/A or genuinely-unapplied-and-gated in `scripts/lint/migration-tracker-baseline.json` → re-run the lint to confirm 0 new drift for that batch → commit+push to the PR branch → report a full per-migration table to orch-console on the bus (`mig | ceayj | goumlyne | referenced-by-main? | action`).
5. **ESCALATE IMMEDIATELY, don't batch it**, any migration that's NOT applied (or one-silo-only) whose objects are referenced by live code on `main` (`grep -rn <table/fn/column> src/`) — that's the "deployed depends on unapplied migration" class, a real production defect. This session found 2 (mig199, mig201) and both are now fixed — see below. There could be more in batches 3-4.
6. The self-declared "UNAPPLIED BY DESIGN" / "APPLY GATED" language in a migration's own header is NOT reliable — ground truth (the actual DB state) overrode a stale header in several cases this session (147, 149, 164, 205 were all self-declared "propose-only"/"gated"/"hold" but actually already live). Always verify at source, never trust the comment.
7. Regex extraction WILL miss things (multi-word quoted policy names needed a fix mid-session; CHECK constraints, storage.* schema objects, and GRANT/REVOKE-only migrations need manual pg_proc.proacl / pg_class.relacl checks, not the generic extractor). Treat the probe script's output as a triage aid, not ground truth — manually verify anything that isn't a clean ALL-PRESENT-both.
8. Watch for migration SUCCESSION: several migrations were "genuinely never applied under their own name" but their intent is live via a LATER migration that superseded them with a richer/renamed version (125→234, 232→240, 235's SELECT policy→256). Before treating a NONE-PRESENT/absent case as real drift, grep the whole migrations dir for the exact policy/object name to see if it moved under a new name. If found: mark the earlier one RETIRED (header note + baseline entry, same convention as 125/232 in the PR history), not "unapplied."

### What's already fully resolved (don't re-litigate)
- Original 31 (#103-165): all resolved. #123 baselined (genuinely unapplied, cai-out-of-irsyad-governance doesn't change that this one specifically has no ack yet). #125, #232 RETIRED (superseded). #164 RATIFIED live by orch-console (documented in its header + `docs/irsyad-requirements-register.md`).
- Batch 1 (#166-220): all resolved. #180 baselined (genuinely gated, no code ref, do not apply — money-adjacent CAI §6.6, self-gated against a real out-of-order hazard).
- Batch 2 (#221-262): all resolved.
- **mig199 (issue_receipts_for_donations) and mig201 (relink_donation) were LIVE-APPLIED to ceayj this session** (orch-console GO msg #40465, gated with an in-transaction proACL assertion, smoke-tested with random/nonexistent ids touching zero real rows, tracker-recorded, verified on a separate connection). Full transcript in bus msg #40479. This was a real production fix, not just bookkeeping — bank-import auto-receipt-issuance and the donor-relink admin dialog were both silently/loudly broken on ceayj before this.

### Key facts to re-derive fast, don't rediscover
- ceayj (`ceayjeamtmcyzzvqflus`) = ihsanos multi-tenant DB. goumlyne (`goumlynecruxrlmzlntp`) = irsyad silo. RO DSNs: `IHSANOS_PROD_RO_DATABASE_URL` / `GOUMLYNE_RO_DATABASE_URL` in `/Users/sheikhmusa/wingmen/orchestrator/.env` (role `auditor_ro`). No write-capable direct DSN is provisioned in this environment — writes go through `supabase db query --linked` (Management API, gated by `SUPABASE_ACCESS_TOKEN`), the same mechanism `scripts/db/apply-migration.mjs` uses. `supabase link --project-ref <ref>` switches which silo `--linked` targets; only one at a time per worktree.
- Tables that exist on ceayj but NOT goumlyne: `storefront_delivery_zones`, `storefront_notifications` (irsyad has no storefront/e-commerce feature). Tables that exist on goumlyne but NOT ceayj: `tabung_jumaat_reports`, `tabung_report_notifications` ("jumaat" Friday weekly-report workflow is irsyad-specific; general `tabung_umum_tins` fund-tracking IS shared). `donations`, `receipts`, `persons`, `org_members`, `audit_log` are shared/both-silo tables.
- `docs/irsyad-requirements-register.md` (owned by cc-irsyad-coord, but orch-console directed writes to it directly this session for a couple of findings) already documents LIVE status for several mig-numbers — cross-check it before assuming you need to derive everything from scratch.

## Standing rails (unchanged from prior handoffs, still true)
- Never `supabase db push` (decision 962). `supabase db query --linked --file <sql>` is the sanctioned write path (Management API, not the shadow-diff push path).
- Every DB write must be verified on a SEPARATE connection after, not same-session/same-transaction visibility.
- **This worktree's path is `/Users/sheikhmusa/wingmen/projects/orchestrator.wt-cleanup`** for the orchestrator lane itself — the ihsanos reconcile work lives in the SEPARATE worktree `~/wingmen/projects/ihsanos-migtracker-reconcile` (a different repo entirely). Always `pwd` before an absolute-path Write/Edit; don't confuse the two, and don't touch `~/wingmen/projects/ihsanos` (the shared main checkout with someone else's uncommitted work).
- Identity: verify `CC_BASE_AGENT_ID`/`CC_AGENT_ID` off your live pid every boot (a `switch_lane_token.sh` bug earlier this session briefly mis-registered this lane as `cc-orchestrator`; fixed in code, but re-verify anyway per standing doctrine).
- 058b and the 66-table default-DML sweep (separate, older mandate) remain orch-console's to commission — not touched this session, not related to the migration-tracker work.

## First actions on boot
1. Reconcile `agent_messages WHERE to_agent='cc-substrate' AND read_at IS NULL`.
2. Verify identity (`CC_BASE_AGENT_ID` should read `cc-substrate`).
3. `cd ~/wingmen/projects/ihsanos-migtracker-reconcile && git status` — should be clean at c59c5307 (or later if you've continued).
4. Resume the tail at batch 3 per the steps above.
