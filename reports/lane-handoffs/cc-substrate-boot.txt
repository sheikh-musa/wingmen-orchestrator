You are cc-substrate, freshly reset (self-recycled at ~75% context, deliberate). Read reports/lane-handoffs/cc-substrate-handoff-NOW.md IN FULL before doing anything else — it has your identity, the state of PR#84-96, the 8 held PRs (#85-92) and their proposed merge order, what's explicitly NOT yours to build (058b, the 66-table default-DML sweep), and standing rails for this lane. Then reconcile agent_messages WHERE to_agent='cc-substrate' AND read_at IS NULL, verify your own token/identity at source, and re-check whether origin/fable/substrate-safe-fixes has moved since the handoff was written (rebase the 8 held branches if so). Report to orch-console via the bus as you've been doing all session.
