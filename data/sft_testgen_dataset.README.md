# SFT test-generation dataset (Claude -> Qwen distillation)

**What this is.** A supervised-fine-tuning dataset of `(task -> gold pytest suite)` pairs in QwenCloud SFT chat format (`{"messages": [system, user, assistant]}`, one JSON object per line, UTF-8, final-answer-only — no `<think>` field). Upload `sft_testgen_dataset.jsonl` to QwenCloud's Create-dataset flow to distil Claude's unit-test-writing capability into a smaller model (Qwen).

**Targets are Claude-verified-good.** Every assistant target was generated by `claude-opus-4-8` (max_tokens=4096) and kept ONLY if it passed the deterministic grader in `scripts/qwen_capability_testgen.py`: it must COMPILE (`ast.parse`), PASS-ON-CORRECT (exec the reference impl + suite in an isolated namespace, every `test_*` passes) and — where the specimen ships seeded mutations — catch at least one (MUTATION-CATCH). Everything else was discarded.

**GOVERNANCE — SYNTHETIC / non-PII only.** Every specimen is authored in-harness: generic Python functions (parsing, validation, small algorithms, date/money/string logic). NONE is client data and NONE is repo code. A client's rows must NEVER enter a training file (TENANT-RESIDENCY-001). This dataset is safe to upload precisely because it contains no tenant rows.

## Contents

- **Rows (gold pairs):** 49
- **Format validates for QwenCloud SFT:** yes
- **Generator model:** `claude-opus-4-8` (Anthropic SDK, max_tokens=4096, N_SAMPLES=3 per specimen)
- **Opus token cost:** in=17479, out=92028 tokens -> **$2.3881** (@ $5/$25 per 1M in/out, list price)

## Specimen list (rows kept per specimen)

| specimen | source | rows |
|---|---|---|
| `slugify` | base | 3 |
| `retry_backoff` | base | 3 |
| `valid_date_range` | base | 3 |
| `parse_csv_row` | base | 3 |
| `state_transition` | base | 2 |
| `round_money` | base | 1 |
| `merge_intervals` | base | 1 |
| `token_bucket_allow` | base | 3 |
| `valid_email` | base | 0 |
| `valid_phone` | base | 3 |
| `pagination_offset` | base | 3 |
| `clamp` | base | 3 |
| `fizzbuzz` | base | 2 |
| `is_leap_year` | base | 3 |
| `parse_query_string` | added | 3 |
| `is_palindrome` | added | 3 |
| `word_count` | added | 3 |
| `celsius_to_fahrenheit` | added | 3 |
| `validate_password` | added | 0 |
| `days_between` | added | 2 |
| `format_thousands` | added | 0 |
| `rle_encode` | added | 0 |
| `binary_search` | added | 2 |
| `luhn_check` | added | 0 |
| `title_case` | added | 0 |
| `chunk_list` | added | 0 |
| `hex_to_rgb` | added | 0 |
| `mean` | added | 0 |

_Base specimens are reused from `qwen_capability_testgen.py`; added specimens are new in `harvest_sft_dataset.py`._
